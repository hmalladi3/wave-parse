[package]
name = "wave-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Zero-copy WiFi CSI frame parsing, subcarrier extraction, and a lock-free SPSC frame ring."

[dependencies]
ring = "0.17"
